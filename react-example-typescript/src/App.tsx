import React from 'react';

interface AppProps {
  // Props 타입 정의
}

const App: React.FC<AppProps> = (props) => {
  return (
    <div>
      {/* 컴포넌트 코드 */}
      typescript
    </div>
  );
};

export default App;
